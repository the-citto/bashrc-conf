# ~/.bashrc
## WSL
`sudo nvim /etc/hostname`
    
    ArchLinux

`sudo nvim /etc/wsl.conf`

    [network]
    hostname = ArchLinux
    generateHosts = false

`sudo nvim /etc/hosts`

    127.0.0.2    ArchLimux.localdomain    ArchLinux

## kitty

